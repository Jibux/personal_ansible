---
name: nas
properties:
  type: smb
  host: nas
  user: "{{ (lookup('file', secrets_path + '/nas/credentials.yml')|from_yaml).jibux.username }}"
  pass: "{{ (lookup('file', secrets_path + '/nas/credentials.yml')|from_yaml).jibux.password_obscure }}"
